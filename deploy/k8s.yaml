apiVersion: apps/v1
kind: Deployment
metadata: { name: highlyagent-api }
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: api
        image: highlyagent/api:latest
        ports: [{ containerPort: 8000 }]
